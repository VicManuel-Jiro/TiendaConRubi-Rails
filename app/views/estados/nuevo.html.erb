<h1>Formulario estado</h1>
<%= form_with(model: @estado, id: 'formulario_estados',url:crear_estado_path, local: true) do |form|%>
<%=render 'formulario',estado: @estado,form: form %>
	<%end%>
	<%= link_to 'Regresar', estados_inicio_path %>















